import styles from "./contact.module.css";

export default function ContactPage() {
  return (
    <main className={styles.main}>
      <h1 className={styles.title}>Contact</h1>
      <p>Pages Router is working.</p>
    </main>
  );
}
